{{- $jump := "" -}}
{{- if hasKey . "jumpSshHost" -}}
{{-   $jump = .jumpSshHost -}}
{{- end -}}
{
  "url": "https://ntfy.internal.cartwmic.com/pi",
  "maxExcerptChars": 200,
  "enabled": true,
  "herdrJumpDeepLinkBase": "termux://herdr-jump"{{ if $jump }},
  "jumpSshHost": {{ $jump | quote }}{{ end }}
}
