{
  "revision": "1",
  "problem": "While the on-call settlement operator checks completed batch `settlement-42`, its result says success despite one rejected settlement.",
  "outcome": "The on-call settlement operator sees failure when completed batch `settlement-42` contains a rejected settlement.",
  "acceptance": [
    "A rejected settlement makes the completed batch result failure",
    "The result names batch `settlement-42`"
  ],
  "non_goals": [
    "Changing settlement validation",
    "Retrying the batch automatically"
  ]
}
