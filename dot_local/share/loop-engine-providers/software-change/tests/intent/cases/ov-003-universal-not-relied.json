{
  "revision": "1",
  "problem": "A batch operator sees a clean result after one record fails, so retry automation does not retry the affected batch.",
  "outcome": "The batch operator can distinguish an all-success batch from a batch containing any failed record.",
  "acceptance": [
    "An all-success batch reports success",
    "A batch containing any failed record reports failure and identifies the affected batch",
    "Everything else behaves exactly as today"
  ],
  "non_goals": [
    "Changing record processing",
    "Changing retry policy"
  ]
}
