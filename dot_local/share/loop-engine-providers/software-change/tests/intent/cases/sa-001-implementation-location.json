{
  "revision": "1",
  "problem": "A batch operator receives success after a failed record, so deployment automation does not retry the affected batch.",
  "outcome": "The operator receives failure for any partially failed batch, and the change that produces this result must be implemented in `src/gates/design.rs`.",
  "acceptance": [
    "Any failed record makes the completed batch result failure",
    "The required change location is `src/gates/design.rs`"
  ],
  "non_goals": [
    "Changing individual-record processing",
    "Adding automatic retry behavior"
  ]
}
