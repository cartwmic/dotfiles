{
  "revision": "1",
  "problem": "A deployment script consumes the batch command exit status and retries only exit status 7. The command currently exits 0 after partial failure, so the script does not retry.",
  "outcome": "The deployment script receives exit status 7 when any batch record fails.",
  "acceptance": [
    "A run where every record succeeds exits 0.",
    "A run where any record fails exits 7."
  ],
  "non_goals": [
    "Changing record processing",
    "Adding retry behavior"
  ]
}
