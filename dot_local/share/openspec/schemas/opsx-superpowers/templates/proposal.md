## Why

<!-- 1-2 sentences. What problem does this solve? Why now? Cite specific
constitution principles or domain invariants if they drive the motivation. -->

## What Changes

<!-- Bullets. Be specific. Mark breaking changes with **BREAKING**.
Reference capability names that will be touched. -->

- <change>
- <change>

## Capabilities

### New Capabilities
<!-- Each becomes specs/<name>/spec.md. Kebab-case. -->
- `<kebab-name>`: <brief description>

### Modified Capabilities
<!-- Existing capability folder names from openspec/specs/. Only list if
spec-level behavior changes (not just implementation). Each needs a delta
spec file. Leave empty if no requirement changes. -->
- `<existing-name>`: <what requirement is changing>

## Impact

<!-- Affected code, APIs, dependencies, systems. Include explicit "Affected
files" subsection when known. Include "Affects which projects" if this is a
cross-repo concern. -->
