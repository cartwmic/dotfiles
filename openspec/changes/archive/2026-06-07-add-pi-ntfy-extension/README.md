# add-pi-ntfy-extension

Local pi extension: on every agent_end, push an ntfy notification (self-hosted) with session name + assistant message excerpt so the user on Android/Termux knows which remote pi session awaits input
