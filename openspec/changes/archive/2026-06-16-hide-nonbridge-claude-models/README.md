# hide-nonbridge-claude-models

Runtime patch (personal profile only) to hide all non-claude-bridge Claude models from pi's model picker / pi --list-models while preserving anthropic auth for web tools and pi-sub-bar
